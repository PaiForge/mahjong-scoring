/**
 * 点数計算練習で出題条件として選ばせてよい役の allowlist
 * 出題可能役リスト
 *
 * 生成器（generator.ts）は「ランダムに作って条件に合わなければ捨てる」
 * rejection sampling なので、`requiredYaku` はプールに十分現れる役でしか
 * 実用にならない。役を自由に選ばせると、生成器がそもそも作れない役
 * （国士無双・大三元などは構造上生成されない）や出現率が 0.1% を切る役
 * （純全帯么九・三槓子）を指定でき、リトライを使い切って黙って失敗する。
 *
 * 収録基準: 既定オプションでの出現率が 2% 以上であること。
 * `generateValidScoreQuestion` を既定オプションで 4,000 回実行し、
 * `yakuDetails` に各役が現れた割合を実測した（2026-08 時点）:
 *
 * - 門前清自摸和 38.9% / 断么九 28.6% / 役牌 白・發・中 各 9〜10% /
 *   立直 9.6% / 一盃口 9.0% / 三暗刻 8.1% / 平和 5.7% / 混一色 4.6% /
 *   対々和 3.3% / 自風牌 2.7% / 場風牌 2.5%
 * - 閾値未満で除外: ダブル立直 1.1%（立直で代替可能）・清一色 1.6%・
 *   三色同順 1.2%・混全帯么九 0.8%・四暗刻 0.8%・連風牌 0.8%・
 *   一気通貫 0.5%・二盃口 0.1%・純全帯么九 0.07%・三槓子 0.05%
 * - ドラ・裏ドラは高頻度だが役ではないため対象外
 *
 * 七対子だけは実測に載らない（既定オプションでは面子手しか作らないため
 * 出現率 0%）。ただし生成器に専用の生成経路があり、`requiredYaku` で
 * 名指しされたときは七対子を作る（generator.ts の `isChiitoi`）ので、
 * 指定すれば確実に出せる。基準は「生成器が安定して作れるか」であって
 * 既定の分布そのものではないため、ここに載せている。
 *
 * 出現率 2% はリトライ 500 回で取り逃し確率 0.004%（(1-0.02)^500）。
 * 閾値未満の役を足したくなったら、閾値を下げてリトライを増やすか、
 * 生成器の面子重み（strategies/）を条件付きで変えるかをこの実測を
 * 基準に判断すること。生成器の分布を変えた場合は上の実測も取り直す。
 *
 * 値の語彙は `yakuDetails.name`（日本語表示名）。並びは既定の役順
 * （yaku/constants.ts の YAKU_DEFAULT_ORDER = 実戦出現率順）に合わせている。
 */
export const SCORE_FILTERABLE_YAKU: readonly string[] = [
  "立直",
  "門前清自摸和",
  "断么九",
  "平和",
  "一盃口",
  "三暗刻",
  "七対子",
  "対々和",
  "混一色",
  "役牌 白",
  "役牌 發",
  "役牌 中",
  "場風牌",
  "自風牌",
];
