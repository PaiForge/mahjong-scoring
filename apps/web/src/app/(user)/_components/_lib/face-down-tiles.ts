/**
 * 暗槓を並べるときに裏向きにする位置
 * 暗槓の裏向き位置
 *
 * 暗槓は 4 枚のうち両端を伏せて置くのが卓上の作法で、それが「暗」槓である
 * ことの目印になる（明槓との区別がこの見た目だけで付く）。牌を 4 枚並べる
 * 側がその都度位置を決めると、用語集と教本で伏せ方が食い違って別の形に
 * 見えてしまうため、位置はここで 1 つに決める。
 *
 * `TileSet` / `GlossaryTermExample` の `faceDownIndexes` にそのまま渡す。
 */
export const CLOSED_KANTSU_FACE_DOWN = [0, 3];
