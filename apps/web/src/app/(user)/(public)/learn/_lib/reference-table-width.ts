/**
 * 教本の小さな早見表の最大幅
 *
 * 「牌 / 種類 / 符」「種類 / 符」のように、中身が短い語と数値だけの表。
 * 本文と同じ全幅（max-w-3xl の 770px 前後）まで伸ばすと、値を持つ列が
 * 右端へ追いやられて行が追えなくなる（desktop 実測: `/learn/jantou-fu` の
 * 例示表は 100/615/55 で、「白」と「2符」の間が 550px 空いていた）。
 * 表の役目は同じ行の値どうしを結びつけることなので、間延びさせるより
 * 幅を絞る。
 *
 * 狭い画面では全幅のまま（絞る余地が無い）。列の多い数表（符×翻・満貫
 * 以上・翻の早見表）は対象外で、あちらは全幅を使いきる。
 */
export const REFERENCE_TABLE_MAX_WIDTH = "sm:max-w-md";
