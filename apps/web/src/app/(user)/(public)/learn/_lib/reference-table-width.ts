/**
 * 教本の小さな早見表の最大幅
 *
 * 「牌 / 種類 / 符」「種類 / 符」のように、中身が短い語と数値だけの表。
 * 本文と同じ全幅（max-w-3xl の 770px 前後）まで伸ばすと、値を持つ列が
 * 右端へ追いやられて行が追えなくなる（desktop 実測: `/learn/jantou-fu` の
 * 例示表は 100/615/55 で、「白」と「2符」の間が 550px 空いていた）。
 * 表の役目は同じ行の値どうしを結びつけることなので、間延びさせるより
 * 幅を絞る。
 *
 * ブレークポイントは付けない。本文の幅が 448px を下回る狭い画面では
 * `max-w-md` は何もしないので、「狭い画面では全幅」は接頭辞なしで
 * 自然に成り立つ。`sm:` を付けると 480〜639px だけ全幅に戻り、639px で
 * 表が 607px・種類の列が 478px と desktop（287px）より間延びしたうえ、
 * 640px で 448px へ縮む段差ができる（実測）。列の多い数表（符×翻・満貫
 * 以上・翻の早見表）は対象外で、あちらは全幅を使いきる。
 */
export const REFERENCE_TABLE_MAX_WIDTH = "max-w-md";
